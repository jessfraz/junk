{
    "defaultAction": "SCMP_ACT_ALLOW",
    "architectures": [
        "SCMP_ARCH_X86"
    ],
    "syscalls": [
        {
            "name": "setuid",
            "action": "SCMP_ACT_ERRNO"
        },
        {
            "name": "unlink",
            "action": "SCMP_ACT_ERRNO"
        }
    ]
}
